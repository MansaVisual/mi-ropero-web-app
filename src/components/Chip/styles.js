import { Box, styled } from "@mui/material";

export const StyledChip = styled(Box)(({ theme }) => ({
  borderRadius: "100px",
  height: "40px",
  whiteSpace: "nowrap",
  display: "inline-block",
}));
